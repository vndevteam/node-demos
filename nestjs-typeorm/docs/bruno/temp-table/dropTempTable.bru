meta {
  name: dropTempTable
  type: http
  seq: 2
}

delete {
  url: {{host}}/temp-table/{{sessionId}}
  body: none
  auth: inherit
}

vars:pre-request {
  sessionId: 1
}

settings {
  encodeUrl: true
}
