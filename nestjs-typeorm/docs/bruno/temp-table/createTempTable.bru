meta {
  name: createTempTable
  type: http
  seq: 2
}

post {
  url: {{host}}/temp-table/{{sessionId}}
  body: none
  auth: inherit
}

vars:pre-request {
  sessionId: 1
}

settings {
  encodeUrl: true
}
